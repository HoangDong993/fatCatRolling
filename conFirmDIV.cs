using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class conFirmDIV : MonoBehaviour
{
	public int status;
    // Start is called before the first frame update
    void Start()
    {
        //  FindObjectOfType<conFirmDIV>().gameObject.SetActive(false);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
